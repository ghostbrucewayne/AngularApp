import { ArrangeorderPipe } from './arrangeorder.pipe';

describe('ArrangeorderPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrangeorderPipe();
    expect(pipe).toBeTruthy();
  });
});
